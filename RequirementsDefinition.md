# 日課管理アプリ 要件定義書

作成日:2026-07-28
作成者:Claude(要件確認フェーズ)/ 開発担当:Claude Code(Opus 5 オーケストレーター体制)

---

## 1. 概要

日々のタスク遂行を通じて設定した目標の達成を管理する、個人利用専用のPWA(Progressive Web App)。
本日実施すべきタスクの確認・進捗登録、目標全体の達成率確認、タスクの新規登録、および毎日決まった時刻でのリマインド通知を行う。

## 2. 対象ユーザー・利用範囲

- 利用者は開発者本人のみ(マルチユーザー機能は不要)
- 利用プラットフォーム:Windows(PC)、iPhone、iPad
- 認証:Googleログインによる本人確認のみ(不正アクセス防止目的)

## 3. 用語定義

要件検討時に用語の混同が発生したため、以下の階層構造で統一する。

| 用語 | 定義 |
|---|---|
| **ジャンル** | 複数の「目標」を束ねる分類。視認性向上が目的で、全ての目標に設定する必要はない(任意項目)。例:「日常タスク」というジャンルに「筋トレを習慣化する」「資格を取得する」等の目標をまとめる |
| **目標** | タスクを積み重ねることで最終的に達成したい到達点。達成率(%)を持ち、100%以上で自動アーカイブされる |
| **タスク** | 目標を達成するために、特定の日付・曜日に実施する具体的な行動。目標に対して個別に「配置」される |

## 4. 機能要件

### 4.1 ホーム画面(本日のタスク閲覧・進捗登録)

- アプリ起動時の初期画面とする
- 本日実施すべきタスクを一覧表示する
- タスクは登録時点で「未達成」に分類される
- ステータスは「未達成」「達成済み」の2種類のみ(「実行中」は不要と確定)
- 進捗の登録方法は以下の2系統を両立させる
  - **アプリ内UI(カウントアップ/ストップウォッチ)**:達成回数または達成時間が設定されたタスクに対し、アプリ内で直接カウントアップ・計測できるUIを提供する。他の計測アプリを併用する必要がない設計とする
  - **手動入力**:上記UIを使わず、数値を直接入力する形でも進捗登録を可能にする
- 目標達成に必要な回数・時間に到達した時点で、自動的に「達成済み」へ変更する
- **選択的達成タスク(グループ化タスク)**
  - 「他のタスクとの選択的連携」として、同一グループに複数タスクを登録できる(2つに限らず3つ以上も可)
  - グループ内で「達成扱いにするために必要な最小達成個数」を数値で設定できる(例:3択中2つ達成で成立)
  - 条件を満たした時点でグループとして達成済みとなり、未達成のまま残った他タスクは「対象外」ステータスとする
  - 「対象外」のタスクは非表示にはせず、灰色文字等の表示で視覚的に区別する
  - 達成率の計算には、実際に達成として計上されたタスクのみを加算し、「対象外」となったタスクは計上しない

### 4.2 進捗管理画面(目標全体の管理)

- 各目標について、日々のタスク達成状況を積み上げた「目標全体の達成率」を閲覧できる
- 達成率の算出ロジックは**未確定**(下記10章「確認事項」参照)。実装前に開発者本人への確認を必須とする
- 達成率が100%以上に到達した目標は自動的にアーカイブする
  - アーカイブ時点でユーザーに「達成した旨」および「アーカイブされた旨」を明示的に通知・表示する(トースト表示等)
- アーカイブされた目標は、いつでも一覧として振り返り閲覧できる画面を別途用意する

### 4.3 目標登録

登録時に設定する項目:

- 目標名
- 目標の進捗定義(100%到達に必要な達成回数 または 達成時間)
- 全体達成率の目標値(任意設定可能)
- 分類するジャンル(任意項目、必須ではない)

以下は目標登録時点では**設定不要**とし、後述4.4のタスク設定画面で個別に設定する。

- タスクとして表示する日時・曜日
- デイリータスクとして表示する際の表示名

### 4.4 タスク設定(目標からタスクへの展開)

目標登録画面とは別に、タスク設定専用画面を設ける。この画面で以下を設定する。

- どの目標を、どの日付・曜日に配置するか(基本は曜日指定、個別の日付指定も可能な柔軟設計)
- デイリータスクとして表示する際の表示名
  - デフォルトは目標名をオートフィル(自動転記)する
  - 個別に変更したい場合は上書き編集を可能にする
- 「他のタスクとの選択的連携」の設定(4.1参照、選択的達成グループへの登録)

### 4.5 通知機能

- 通知の送信有無を選択できる(デフォルト:オフ)
- 通知をオンにした場合、以下2種類それぞれについて個別にオン/オフを選択できる
  1. 指定時刻に本日のタスク一覧を通知する
  2. 指定時刻に現在の進捗状況(未達成タスクを中心とした見やすい形式)を通知する
- 各通知の送信時刻は、それぞれ独立して設定できる
- 通知フォーマットの細部デザインは開発側に一任する。ただし②は「未達成タスクが見やすいこと」を満たすこと

## 5. 通知インフラ実装方式

### 5.1 採用方式:Firebase Cloud Functions + Cloud Scheduler(Blazeプラン)

検討の結果、GitHub Actions cron + FCM直接送信という代替案と比較し、以下の理由から**Cloud Functions + Cloud Schedulerを採用**する。

| 比較項目 | Cloud Functions + Scheduler(採用) | GitHub Actions cron(不採用) |
|---|---|---|
| 時刻精度 | Cloud SchedulerはSLAに基づき指定時刻通りに実行 | ベストエフォートであり、実運用で数十分〜数時間の遅延事例が報告されている。日課の定時通知という要件の性質上、不向きと判断 |
| 費用 | Blazeプラン(従量課金)化が必須だが、後述の通り実費用は¥0 | Firestoreは無料のSparkプランのままで良い |
| 実装の一貫性 | Firebase内で完結 | サービスアカウント鍵をGitHub Secretsとして別途管理する必要があり、公開リポジトリ運用との相性でリスクが増える |

### 5.2 費用試算

1日2回・月間約62回の通知生成では、以下の理由から実費用は発生しない。

- Cloud Functions:月200万回の呼び出しまで無料。62回はその0.003%程度
- Cloud Scheduler:Googleアカウントあたり3ジョブまで無料。通知2種類=2ジョブで収まる
- Firestore・Storage等も無料枠(Sparkプランの日次クォータ)を大幅に下回る想定

### 5.3 安全対策

Blazeプランには支出のハード上限が存在しないため、想定外の高額請求を防ぐ目的で、Google Cloudの**予算アラート(無料機能)を月$1程度で設定**することを推奨する。これは開発初期段階で必ず設定すること。

## 6. 非機能要件

### 6.1 PWA化

- ホーム画面追加(インストール)によりiOS/iPadOS上でもWeb Push通知を受信できる構成とする
- アプリアイコン、マニフェスト等は開発側に一任する

### 6.2 セキュリティ

- GitHub Pagesは公開リポジトリでの運用が前提となるため、以下を厳守する
  - Firebase設定値(APIキー等のクライアント公開情報)以外の機密情報(サービスアカウント鍵等)をリポジトリに含めない
  - Firebase Authentication(Googleログイン)によるアクセス制御を実装し、本人以外のデータ閲覧・書き込みを防止する
  - Firestoreセキュリティルールで、認証済み本人のUIDに紐づくデータのみ読み書き可能に制限する
- リリース前に、公開リポジトリ化に伴うセキュリティレビューを必ず実施する

### 6.3 対応プラットフォーム

- Windows(PC・主要ブラウザ)
- iPhone / iPad(Safari、PWAとしてホーム画面に追加した状態を前提)

## 7. 技術スタック

- ホスティング:GitHub Pages
- バックエンド:Firebase(Firestore、Cloud Functions、Cloud Scheduler、Authentication)
- 通知:Firebase Cloud Messaging(FCM)
- フロントエンド形式:PWA

## 8. 開発体制・プロセス

- 開発環境:PCのWSL2 + VSCode上のClaude Code
- 開発体制:Claude Codeにおいて **Opusをオーケストレーター(司令塔)** として配置し、Opusの判断で実装タスクを各サブエージェント(モデル)に配分する
  - 例:定型的な実装作業は軽量モデルへ、設計判断や複雑なロジックはOpus自身または高性能モデルへ、という形でOpusが采配する
  - この配分方針はClaude Code側の`.claude/agents/`設定または明示的な指示によって実現する
- **例外**:以下の2フェーズについては、上記の自動配分ではなく、当初指示通り全モデルを動員して相互確認を行う
  1. 要件定義書作成前の要件確認フェーズ
  2. テストフェーズ
- 要件定義書の最終化(本ドキュメントのブラッシュアップ)はOpus 5が担当する

## 9. データモデル(概略)

実装時の詳細設計はClaude Code側に委ねるが、最低限以下のエンティティ関係を満たすこと。

- `Genre`(任意):1対多で`Goal`を保持
- `Goal`:目標名、達成条件(回数/時間)、目標達成率設定値、ステータス(進行中/アーカイブ済み)
- `Task`:紐づく`Goal`のID、配置日付・曜日、表示名、選択的連携グループID(任意)
- `SelectiveGroup`(任意):グループに属する`Task`のID一覧、達成成立に必要な最小達成数
- `ProgressLog`:日付、対象`Task`、達成回数/時間、ステータス(未達成/達成済み/対象外)
- `NotificationSetting`:通知種別ごとのオン/オフ、送信時刻

## 10. 確認事項(実装着手前に開発者本人へ確認が必要)

- **目標全体の達成率の算出ロジック**:日次のタスク達成率をどのように積み上げて目標全体の達成率とするか(例:期間内の日次達成率の単純平均か、累積達成回数ベースか等)が未確定。実装着手前に必ず確認すること
